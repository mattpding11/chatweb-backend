const JWT_SECRET = process.env.JWT_SECRET;
const PORT = process.env.PORT;
const URL_FRONT = process.env.URL_FRONT;

// Barriel file to import enviroments variables
module.exports = {
    JWT_SECRET,
    PORT,
    URL_FRONT
}
